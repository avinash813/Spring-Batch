server:
  port: 8888
spring:
  batch:
    jdbc:
      initialize-schema: ALWAYS
  datasource:
    password: password
    url: jdbc:mysql://localhost:3306/csvdb
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
